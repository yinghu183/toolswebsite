server {
    listen 80;  # 这里保持 80，因为这是容器内的端口
    server_name tools.577277.xyz;

    location / {
        proxy_pass http://flask-app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
